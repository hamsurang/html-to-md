export const DEFAULT_SETTINGS = {
  includeMetadata: true,
  keepImages: true,
  keepLinks: true,
  siteRules: { github: true, stackoverflow: true, medium: true, notion: true },
  customSelectors: {},
  enabledPresets: {},
};
